# AGENTS.md

本文件定义在本仓库中协作的代码代理（agents）应遵循的默认约束。除非用户明确覆盖，这些规则对仓库根目录及其所有子目录生效。

## 1. 项目定位

- 本项目用于课题研究：端侧 Agentic RAG 优化。
- 工作目标优先围绕真实端侧环境下的 RAG 链路优化，包括但不限于检索、索引、推理、延迟、内存占用、稳定性与可复现实验。
- 做方案选择时，优先考虑“便于实验、便于对比、便于复现”，不要为了工程复杂度引入与当前课题目标无关的大改动。

## 2. 默认设备约束

- 端侧设备固定为通过 `adb` 连接的设备：`fd8657d6`。
- 任何涉及端侧运行、部署、调试、性能测试、日志抓取或验证的操作，默认都应显式面向该设备，例如优先使用 `adb -s fd8657d6 ...`。
- 在开始任何端侧相关操作前，先检查设备状态：

```bash
adb devices
```

- 如果 `fd8657d6` 没有出现在 `device` 状态中，必须立即停止，不要继续执行，不要自行切换到其他设备，不要改用模拟器，也不要假设结果有效。
- 遇到设备不在线的情况时，代理应明确告知用户：当前必须等待用户重新连接设备，然后再继续。

## 3. 当前优先框架

- 当前阶段默认优先采用 `llama.cpp` 作为主 LLM 框架。
- 涉及后端选型、推理链路接入、构建参数选择或默认实现时，优先沿用和增强 `llama.cpp` 路线。
- 若存在多种可选实现，默认优先：
  - `LlamaCpp` 后端
  - `third_party/llama.cpp`
  - 与 `llama.cpp` 兼容的模型与构建方式
- 除非用户明确要求，或当前任务被现有实现阻塞，不要主动把主要工作切换到 `MNN` 或 `MLLM` 路线。

## 4. 代码与实验工作原则

- 优先做小步、可验证、可回退的修改，避免一次性大范围重构。
- 涉及实验配置、性能数据或对比结果时，记录关键前提：设备、模型、后端、构建选项、输入数据与测量方式。
- 如果某项修改只在桌面端验证过，而没有在 `fd8657d6` 上实际验证，必须明确标注“尚未完成目标设备验证”。
- 不要把桌面端结果表述为端侧结果。
- 若任务需要端侧验证但设备不在线，结论只能停留在代码层或主机侧，不能声称端侧验证完成。

## 5. 构建与实现偏好

- 优先复用仓库现有构建方式、目录结构和抽象接口，不随意新增平行体系。
- 若需要指定 LLM 后端，优先使用与 `llama.cpp` 一致的配置，例如 `-DLLM_BACKEND=LlamaCpp`。
- 做实现决策时，优先维护以下目标：
  - 端侧可运行
  - 实验可复现
  - 链路可观测
  - 后续便于做性能优化和消融对比

## 6. 沟通要求

- 汇报端侧相关进展时，应明确说明设备状态：
  - 已在 `fd8657d6` 上验证
  - 未验证，因为设备未连接
- 如果因为设备离线而中断，直接说明阻塞点并等待用户处理，不要自行绕过该约束。

